<div class="card text-center  mb-2">
    <div class="row">
        <div class="col pt-2">
            <blockquote class="blockquote">
                <p> <small> <%= comment.content %></small></p>
        </div>
        <div class="col pt-3">
                <footer class="blockquote-footer">
                Il y a <cite title="Source Title"><%= time_ago_in_words(comment.created_at)%></cite>
                </footer>
            </blockquote>
        </div>

        <div class="col pt-2">
            <% if current_user %>
                <% if current_user.id == comment.user_id %>
                <%= link_to 'Supprimer', [comment.post,comment], method: :delete, data: { confirm: 'Êtes-vous certain.e de vouloir supprimer votre commentaire ?' }, class: 'btn btn-danger' %>
                <% end %>
            <% end %>
        </div> 
    </div>
</div>